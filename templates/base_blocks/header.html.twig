<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid d-flex justify-content-between">
        <a class="navbar-brand" href="/"><i class="fas fa-record-vinyl me-1"></i>Vinylium</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link"
                       aria-current="page"
                       href="{{ path('artists') }}">
                        <i class="fas fa-guitar"></i> Artistes
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link"
                       aria-current="page"
                       href="{{ path('genres') }}">
                        <i class="fas fa-podcast"></i> Genres
                    </a>
                </li>
            </ul>
        </div>
        <div class="d-flex">
            {% if app.user %}
                    {% if admin %}
                        <a class="nav-link text-dark primary-hover"
                           aria-current="page"
                           href="{{ path('admin') }}">
                            <i class="fas fa-user-shield me-1"></i> Admin
                        </a>
                    {% endif %}
                        <a class="nav-link text-dark primary-hover"
                           aria-current="page"
                           href="{{ path('profil',{id:app.user.id}) }}">
                            <i class="fas fa-user-circle me-1"></i> Profil
                        </a>
                        <a class="nav-link text-dark primary-hover"
                           aria-current="page"
                           href="{{ path('collection',{id:app.user.id}) }}">
                            <i class="fas fa-th"></i> Collection
                        </a>
                        <a class="nav-link text-dark primary-hover"
                           aria-current="page"
                           href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt"></i> Se deconnecter
                        </a>
            {% else %}
                    <a class="nav-link text-dark primary-hover" aria-current="page" href="{{ path('app_register') }}"><i class="fas fa-sign-in-alt me-1"></i>S'inscrire</a>
                    <a class="nav-link text-dark primary-hover" aria-current="page" href="{{ path('app_login') }}"><i class="fas fa-user me-1"></i>Se connecter</a>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>

{% for label, messages in app.flashes %}

    <div class="container mt-2">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p class="m-0">{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>

{% endfor %}