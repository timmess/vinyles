{% extends 'base.html.twig' %}

{% block title %}{{ album.artist.name }}{% endblock %}

{% block body %}
    {{ form_start(form) }}

        <div class="alert alert-light">
            <h2 class="h4">Mettre à jour {{ album.name }} de {{ album.artist.name }}</h2>
            <img src="{{ album.photo }}" alt="{{ album.artist.name }}" class="mh-23 my-3">
            <div class="row">
                <div class="my-2">
                    {{form_row(form.name)}}
                </div>
                <div class="my-2">
                    {{form_row(form.photo)}}
                </div>
                <div class="my-2 d-none">
                    {{form_row(form.artist)}}
                </div>
                <div class="my-2">
                    {{form_row(form.genres)}}
                </div>
            </div>
        </div>

        <button type="submit" class="btn btn-success">Enregistrer</button>

    {{ form_end(form) }}
{% endblock %}
